schema_version: v1alpha2
dependencies:
  redis: {enabled: true}
environment:
  REDIS_URL: {from: dependency.redis.url}
  FORCE_DISCONNECTED: {value: "true"}
endpoints:
  health: /health
  readiness: /ready
readiness:
  status: 200
  json: {status: ready}
